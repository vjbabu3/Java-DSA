arr = [1, 2, 2, 3, 1, 2]

visited = []

for i in range(len(arr)):

    if arr[i] in visited:
        continue

    count = 1

    for j in range(i + 1, len(arr)):
        if arr[i] == arr[j]:
            count = count + 1

    visited.append(arr[i])

    print(arr[i], "=", count)
